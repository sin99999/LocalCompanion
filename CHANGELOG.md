# 変更履歴（CHANGELOG）

本ファイルは LocalCompanion の利用者向け変更履歴です。
バージョンは `LocalCompanion.csproj` の `<Version>` と一致させます。

## Unreleased

### 変更

## 1.2.3 - 2026-08-26

公開ドキュメントと配布 ZIP の版を揃え、セキュリティ案内を追加しました。

### 変更

- 公開ドキュメントに SECURITY.md を追加し、README と配布 ZIP から参照できるようにしました
- 既定の挨拶と、キャラクター設定提案の案内文言をです・ますに揃えました
- トラブルシューティング等の「設定」表記から装飾用の記号を外し、画面の案内に揃えました

## 1.2.2 - 2026-08-13

配布 ZIP 名の整理、llama.cpp の自動更新、起動案内とドキュメントを整えました。

### 変更

- 配布 ZIP のファイル名から `-user` 接尾辞をやめ、`LocalCompanion-{バージョン}.zip` に統一しました（作成スクリプトは `scripts\package-zip.ps1`）
- 起動時に導入済みの llama.cpp と GitHub の最新リリースを照合し、古い場合は自動で取り直すようにしました（確認に失敗しても既存ビルドで起動を続けます）
- README / トラブルシューティングに、二重起動の案内、開発起動と配布 ZIP でデータ場所が違うこと、残った llama-server、RAG の OCR 未対応を追記しました

### 修正

- 「会話からキャラクターを育てる」で、容姿・数値・呼び方の合意が提案に薄まる問題を改善しました。直近の複数ターンを参照し、長い応答では末尾の確定描写を優先し、会話中の具体値をヒントとして渡すようにしました
- 大きい GGUF 利用時にキャラクター設定の提案作成が短時間で打ち切られ、ダイアログが出ないことがあったため、待ち時間を延ばし、時間切れ時は状況を表示するようにしました
- 「ルールを提案して」などでも保存用ダイアログが出るよう意図検出を広げ、モデルが提案 JSON を返せないときは会話中の箇条ルールを機械的に取り込むようにしました
- 提案作成中の表示のあと、失敗時に黙って「完了」へ戻らず、理由が分かるメッセージを出すようにしました
- すでに起動しているときに exe を開き直すと、無言で終了せず案内を表示するようにしました
- チャット用の日本語システムプロンプトで「キャラ」と略していた箇所を「キャラクター」に揃えました

## 1.2.1 - 2026-07-25

キャラクター設定を会話から育てる仕組みと、起動から終了までの安定性を整えました。

### 追加

- 設定 → キャラクターに「会話からキャラクターを育てる」を追加しました（既定はオフ）。名前付きキャラクターとの会話のあと、性格・指示の変更案を出すことがあります。提案は毎回確認ダイアログで承認したときだけ保存されます
- 「○○.json に記述して提案して」など明示的に依頼したとき、保存用の確認ダイアログが出やすくなるよう提案処理を強化しました（チャット本文に出る独自の JSON 案とは別です）
- 提案する性格・指示の本文は Markdown（見出し・箇条書き）で書くようにしました。保存先は従来どおりキャラクターの .json 内の文字列です

### 修正

- 起動中にウィンドウを閉じたとき、llama-server が再起動の再試行で再び立ち上がりうる経路を止めました
- 終了時にチャット送信・キャラクター設定の提案・VOICEVOX の発話をキャンセルするようにしました
- VOICEVOX の自動起動がタイムアウトしたあとも、アプリ終了時に自前で起動したプロセスを止めるようにしました
- 「基本設定を初期値に戻す」が、長期記憶や「会話からキャラクターを育てる」まで巻き戻さないようにしました
- モデルが無いときや既定モデルのダウンロードに失敗したときはチャット画面へ進まず、起動画面で再試行できるようにしました
- 開発用の出力フォルダ判定を見直し、配布 ZIP を特定のフォルダ名に置いてもユーザーデータの場所を取り違えにくくしました
- 二重起動を抑止し、llama-server および VOICEVOX の子プロセスを親終了時に止まりやすくしました
- キャラクター設定の提案中は、履歴削除や再送信などと確認ダイアログが衝突しにくくなるようにしました

## 1.2.0 - 2026-07-23

WebView2 会話表示の安定化と、推論 ON 時のリアルタイム可視化を進めました。

### 変更

- 推論 ON のとき、考え中の内容を会話欄に随時表示（点滅カーソル付き）。応答完了後も推論ブロックを残します
- ステータスに「推論中…」を表示します

### 修正

- 推論ストリーム中に画面が進まず、終わってから一括表示になっていた描画タイミングを修正しました
- 応答本文が始まると推論テキストが消えていた不具合を修正しました
- ヘルプ／トラブルシューティングのログ・バックアップ案内を「設定 → このアプリについて」に合わせました
- データフォルダーが書き込めない場合の LocalAppData フォールバックをヘルプ類にも記載しました
- メモリ不足メッセージの表記（システムメモリ）を修正しました
- Web 検索で取得する各ページの上限表記を実装に合わせて整理しました

## 1.1.7 - 2026-07-18

チャット本文の選択クラッシュを避けつつ、発言をまたいだ全文選択ができるようにしました。

### 変更

- 会話ログ表示を WinUI `RichTextBlock` から **WebView2（Chromium DOM）** に切り替え。発言またぎのドラッグ選択・コピーが可能（複数 TextBlock では WinUI 仕様上不可のため）
- **動作環境に Microsoft Edge WebView2 Runtime が必須**になりました（配布 ZIP 非同梱。Windows 11 では通常含まれます。詳細は README / トラブルシューティング）
- WebView2 Runtime が無い PC では起動後に案内ダイアログを表示し、公式ダウンロードページを開けます（会話欄にも案内を表示）
- URL は HTML リンクとして表示し、クリックで既定ブラウザを開きます（リンク上は指カーソル）
- 選択中は HTML の再描画を後回しにし、生成ストリーム中の選択消失を抑えます
- チャット表示で絵文字の異体字セレクタ（例: ❤️ の FE0F）を外し、表示の安定性を上げます

### 修正

- × で閉じるとき、ウィンドウ表示を待たせずに閉じ、記憶抽出は裏で最大約 3 秒だけ試してからプロセス停止（閉じるまでの待ち時間を短縮）
- llama-server 起動済み判定を記録 PID ベースにし、ポート 8080 が他プロセスのときのメッセージを明示
- チャット書き出しで `C:/...` 形式のパスも保存先として認識
- データバックアップ ZIP を一時ファイル経由で置き換え、失敗時に旧 ZIP を消さないように変更
- URL の直後にスペースなしで続く日本語（例: `…/LocalCompanionだよ？`）を URL に含めないよう切り出しを修正

## 1.1.6 - 2026-07-18

チャットから Web の内容を取り込みやすくし、設定画面の表示を実効値に揃えました。

### 追加

- メッセージ内の `http(s)://` URL を自動取得し、添付と同様に回答へ反映
- 「調べて」などの調査意図で Web 検索（既定: DuckDuckGo HTML）し、上位ページの本文も参照
- 検索エンドポイントは `appsettings.json` の `WebSearchBaseUrl` で差し替え可能（SearXNG 等）。`WebSearchEnabled` でオフにもできます

### 変更

- 設定のコンテキスト長スライダー上限を実効ポリシー（16384）に合わせ、希望値と実効値を表示
- デフォルト AI では生成パラメータを編集不可にし、名前付きキャラクターでのみ変更可能に
- コンテキスト変更後にモデル再適用が必要なとき InfoBar を表示
- 設定タブ構成を整理（記憶を基本へ、About 分離、モデル／RAG／生成の詳細を Expander 化）
- RAG 取込で埋め込みに失敗したチャンクも本文と FTS には登録（ベクトルのみスキップ）
- Web 検索結果に【参考URL】一覧を付け、回答末尾で出典 URL を示すよう促す
- 「調べて」等の Web 調査後、参照 URL を返答末尾に確定付与し、会話本文内のリンクとしてクリックでブラウザを開く

### 修正

- URL 取得時にローカル／プライベート宛先を拒否し、リダイレクト先も検査（SSRF 緩和）
- 英語 UI でも RAG「検索に使用」や長期記憶ラベルが日本語のまま残ることがあった問題を修正
- デスクトップ書き出し依頼の「書きだして」（ひらがな）を意図検出できるように修正
- 長期記憶をキャラクターごとに分離し、デフォルト AI では抽出しない／注入しないように変更
- 雑談では RAG を実行せず、法律・資料・危険話題のときだけ参照するよう変更（弱いヒット除外。危険話題は注意口調＋法令資料優先）

### ドキュメント

- プライバシー注記（README / About / トラブルシューティング）に、メッセージ内 URL と Web 検索のアクセスを追記
- Web 検索の既定（DuckDuckGo）・オフ方法（`WebSearchEnabled`）を About／ヘルプ／README で揃えた
- 設定タブ説明の「記憶」表記を実 UI（基本設定内）に合わせて修正
- 配布 ZIP に `CONTRIBUTING.md` を同梱するよう変更

## 1.1.5 - 2026-07-14

終了時の記憶抽出と会話切替の安全性を高めた品質改善です。

### 修正

- アプリ終了時に記憶抽出を待ってから llama-server を停止するよう変更（閉じる直後に記憶が落ちるレースを防止）
- 会話スレッド切替中の終了処理ゲートを切替完了まで保持（高速切替でのセッション不整合を緩和）
- 応答中の「新規会話」で黙って何も起きない問題を、忙しい旨のステータス表示に変更
- VOICEVOX の自動起動が失敗・タイムアウトしたあと、再試行できるように修正
- 無効化した会話検索が設定に残っていても常にオフになるよう正規化

### 変更

- 配布 ZIP 検査で `data\` / `tools\` / `models\selection.json` / ツリー内 `rag.db` の混入を拒否

## 1.1.4 - 2026-07-12

チャット表示の改行まわりを改善しました。

### 修正

- 選択肢ラベル（A. B. C.）が絵文字の直後にくっついて見える問題を修正
- 半角括弧内の「？」で改行が入り、`)B.` のように閉じ括弧が孤立する問題を修正
- 選択肢ラベルの `.` を英語句点として誤って改行しないよう調整

## 1.1.3 - 2026-07-10

使い勝手の統一と、公開後レビューで見つかった残課題の仕上げです。

### 修正

- キャラクター切替／新規会話の終了処理と送信が競合しうる問題を直列化して防止
- セッション終了時・記憶抽出の失敗をログに残すよう改善
- VOICEVOX 設定 JSON が壊れているとき、バックアップを残して既定値へ復旧

### 変更

- 基本設定の「音声入力」「履歴削除の確認」を変更時に自動保存（他タブと揃える）
- VOICEVOX スライダー変更の保存をデバウンス
- 配布 ZIP に `README.md` を同梱し、`appsettings.local.json` を publish 出力から除去
- プライバシー注記に VOICEVOX 更新確認・Hugging Face アクセスを追記
- サードパーティ表記に Microsoft.Data.Sqlite / SQLitePCLRaw を追記
- 未使用の会話検索結果型と孤児ローカライズキーを整理
- キャラクター切替時の履歴ポリシーと検索インデックス削除の回帰テストを追加

### ドキュメント

- トラブルシューティング（HTML / md）のタブ名を「基本設定」に統一
- プライバシー／ネットワーク節を更新

## 1.1.2 - 2026-07-10

公開後レビューに基づく安全側の修正と、設定まわりの使い勝手改善です。

### 修正

- キャラクター切替後に旧セッションへ別キャラクターの履歴が混ざる問題を防止
- 会話セッション削除時に検索用インデックス（FTS / vec）も掃除
- アプリ設定 JSON が壊れているとき、バックアップを残して既定値へ復旧
- 大規模 RAG の旧検索経路で結果が空になる問題を緩和（新しいチャンクから上限件数を評価）

### 変更

- RAG 取込の既定上限を安全側に設定（約 32 MB / フォルダー 500 ファイル。`appsettings.json` で変更可。0 以下は無制限）
- 英語 UI で英語の音声パックが無いとき、日本語認識へフォールバックした旨を案内
- 配布 ZIP に `appsettings.local.json`・個人 GGUF・キャラクター JSON が混入しないよう検査を追加
- RAG 登録一覧の応答を UI 言語に合わせて表示
- 長期記憶の重複保存を抑制し、新規に覚えたときステータスで件数を表示
- 設定 RAG タブのオン／オフ表示を UI 言語に追従
- キャラクター生成パラメータの既定を Gemma 4 推奨に合わせる（温度 1.0、コンテキスト 16384）。最大出力トークンの上限を 16384 に拡張
- VOICEVOX 設定を変更時に自動保存（保存ボタンを撤去）
- 音声入力のトグルを「記憶」から「基本設定」へ移動
- プライバシー注記を更新（更新確認・音声認識の例外を明記）
- サードパーティ表記に PdfPig / BuildTools を同期
- 過去メッセージ検索の起動時 backfill と死んだ UI 文言を整理

### ドキュメント

- トラブルシューティング（HTML / `docs/Troubleshooting.md`）を 1.1.2 向けに更新（音声入力の場所・VOICEVOX 自動保存・キャラクター切替・プライバシー）
- README のネットワーク／プライバシー／VOICEVOX 手順を更新

## 1.1.1 - 2026-07-09

v1.1.0 公開後の不具合修正と、記憶・音声入力まわりの使い勝手改善です。

### 修正

- 音声入力後に VOICEVOX の音量が下がったままになることがある問題（認識終了時にマイクを解放）
- マイクボタンが音声認識モードのまま戻れなくなる問題（再クリックで終了／1回認識で自動終了）
- Windows のマイク許可がオフのとき、2回目以降のクリックで何も出ない問題。毎回マイク設定をオンにするよう案内する
- チャット表示で「！」「？」の直後に絵文字が続くとき、不要な改行が入る問題
- 長期記憶の自動抽出がアプリ終了時しか走らず、保存済みの記憶が増えないことがあった問題（新規会話／履歴切替／キャラクター切替時にも抽出）
- 設定を開いた直後にタブ見出しが左寄せになる問題（初回レイアウト後に等幅タブを再計算）

### 変更

- 左ペインの過去メッセージ検索 UI と設定トグルを撤去（厳密な横断検索より、自動記憶を優先）
- 手動の記憶追加・保存済み一覧を設定から撤去。記憶は内部保存のみで、会話中に自然に思い出して触れる
- .NET 10 で不要になった `System.Text.Encoding.CodePages` の PackageReference を削除（NU1510 警告解消）
- 開発向け: `appsettings.local.json` で VOICEVOX 未導入時の UI テストが可能（`HideInstallForTesting`）

### ドキュメント

- トラブルシューティング（HTML / `docs/Troubleshooting.md`）に記憶・音声入力・設定タブの説明を追加

## 1.1.0 - 2026-07-08

### Added

- **長期記憶**: 会話をまたいでユーザーについて覚えた内容を回答に反映（設定 → 記憶と検索）
- **過去メッセージ検索**: 左ペインの検索ボックスから、キーワード＋意味の近い発言を横断検索
- **音声入力**: Windows 音声認識によるマイク入力（設定で有効化、チャット入力欄のマイクボタン）

## 1.0.7 - 2026-07-07

公開後レビューで見つかった不具合と信頼性の改善です。

### 修正

- RAG ベクトル検索で資料フィルタ使用時に sqlite-vec の KNN 制約エラーが出て検索が無効化される問題を修正
- チャット保存機能以外の返答から正当な「保存しました」等が除去される問題を修正
- 「名前を付けて説明して」等の通常質問が保存リピートと誤認される問題を修正
- 同名ファイルの上書き確認後に LLM 整形をやり直して内容が変わる問題を修正（確認待ち内容を再利用）
- Program Files 等で `{exe}\data` に書き込めない場合は LocalAppData へフォールバック
- `D:\bin\...` 等のパスが開発ビルドと誤判定される問題を修正

### 変更

- 同名ファイル競合時はチャットで「上書き保存」「別名保存」を案内
- 配布 ZIP に `docs\code-signing.md` を同梱（Troubleshooting のリンク切れ解消）
- 内部ドキュメント・システムプロンプトの個人名表記を中立化

### 開発基盤（Agent / テスト）

- 公開前監査・RAG・Core テスト向けの開発用手順を整理
- 並列での公開前チェック手順を整理
- 法令系テストを `Fixtures/PenalCodeTestFixtures` に統一（個人パス非依存）
- 1.0.7 修正の回帰テスト追加（`RagSqliteVecTests`, `ChatExportPendingStoreTests`, `AppPathsTests`）

## 1.0.6 - 2026-07-07

調査結果をデスクトップへテキストファイルで書き出す機能を追加したパッチです。

### 追加

- チャットで「～を調べてデスクトップに置いて」等と依頼すると、回答後にデスクトップへ保存
- 対応形式: `.txt` / `.md` / `.markdown` / `.mdx` / `.rst` / `.csv` / `.json` / `.xml` / `.html` / `.htm` / `.yaml` / `.yml` / `.log` / `.ini` / `.cfg`
- RAG 利用時は出典をファイル末尾（または Markdown の見出し）に付与

### 変更

- デスクトップ保存時に LLM で短いファイル名（タイトル）と読みやすい本文へ再整形（チャット表示は従来どおり）
- デスクトップ書き込みの検証・ログ出力を追加。LLM の偽「保存しました」表示を除去し、アプリ側の保存結果のみ通知
- デスクトップに書き込めない環境では `data\exports\` へフォールバック
- 保存先を拡張: フォルダーの絶対パス、`ドキュメント` / `ダウンロード` / `data` / アプリフォルダ、USB メモリ（複数時は一覧を表示してドライブ指定を案内）
- 「もう一度お願い」等のリピート依頼で、直前の保存付き依頼を引き継ぎ

## 1.0.5 - 2026-07-06

RAG の入口統一・PDF 読取・FAQ 検索・引用優先応答を追加したアップデートです（V2 向け Phase 3–5 の一部）。

### 追加

- チャット URL ダイアログから「RAG に登録」
- 短いテキスト添付（3000 文字以下）と RAG 検索の併用
- チャットへのテキストファイル ドラッグ＆ドロップ
- CitationFirst 応答モード（条文質問で引用優先プロンプト）
- FAQ 意図検索・VERBATIM（Q/A 形式 ingest）
- `IDocumentReader` レジストリと PDF レイアウト解析 Reader（Settings トグル）
- Shift_JIS テキストのエンコーディング検出
- 取込レポート（種別・定義/FAQ/条文件数）を Settings RAG タブに表示

### 修正

- Markdown 法令形式の条文メタデータ（`article_sort_key`）を `#### 第N条` 形式から確実に抽出
- 構造化検索失敗時に見出し前方一致（`第8条…`）へフォールバック
- VERBATIM 失敗時に LLM が創作条文を返さないガード
- 「RAG に何が登録されてる？」系の資料一覧を Settings と同じ内容で返す
- ソースヒント付き検索の hybrid フォールバックを対象資料に限定
- UTF-8 法令 `.md` が Shift_JIS と誤判定され条文メタデータが失われる問題を修正
- 一般資料向け: 登録ソース名・URL からの動的ヒント、法令以外への条文意図誤ルーティング防止
- プログラミング言語・金融用語向けの定義質問パターンと取込拡張子（`.cpp` / `.rb` / `.go` 等）を追加

## 1.0.4 - 2026-07-04

RAG の検索・引用・キャラクター会話連携、および資料取込パイプラインを大幅に強化したアップデートです。

### 追加

- RAG ハイブリッド検索（FTS5 + ベクトル RRF）と条文・罰則・定義・相談向けの意図別検索
- 高信頼時の VERBATIM 応答（罰則・条文・用語定義で LLM をバイパス）
- キャラクター選択時の RAG 会話モード（口調を保った資料引用・複数資料相談）
- 取込時の資料種別判定・汎用メタデータ（用語キー・定義文など）
- 設計ドキュメント `docs/RAG-Universal-Architecture.md`
- HTML 取込時の Markdown 風構造化（見出し・箇条書き）
- 設定「取込時にローカル AI で構造化」（PDF/HTML 等を llama-server で整形してから RAG 登録）
- AI 整形結果のキャッシュ（`rag-cache/`・元ファイルは変更しません）
- 設定 RAG タブから URL を直接 RAG 登録

### 変更

- RAG 取込パイプラインをクエリプランナーと構造化検索に再編
- 複数資料を横断する相談クエリの検索を改善
- HTML ファイルはタグ除去前の生 HTML を ingest パイプラインで処理

## 1.0.3 - 2026-07-03

Vision 対応モデルの互換性を中心としたパッチです。

### 追加

- Qwythos-9B-Claude-Mythos 系 GGUF 向け mmproj ファミリーを追加（モデル選択時に mmproj を自動取得）

## 1.0.2 - 2026-07-03

使い勝手と資料活用の向上を中心としたマイナーアップデートです。

### 追加

- 起動時のアプリ更新確認（GitHub Releases を確認し、新バージョンがあればダウンロードページへの案内を表示。24時間に1回まで。「後で」で該当バージョンの再通知を抑制）

### 変更

- RAG がオンのとき、質問に関連する資料が検索で見つかった場合のみ、参考資料を優先して根拠を示すようシステム指示を強化（関連しない質問や資料がない場合は従来どおり自然な会話）

## 1.0.1 - 2026-06-29

安定性と使い勝手の改善を中心としたマイナーアップデートです。

### 変更

- キャラクター設定の「話し方・口調」入力欄を廃止し、性格・指示（persona）に統合（既存 JSON の `speakingStyle` は引き続き読み込み）

### 修正

- × で終了したときにウィンドウがすぐ閉じないことがあった問題（バックグラウンドで llama 等を停止するよう復帰）
- 応答生成中の会話切り替えや設定操作で不具合が出ることがあった問題
- RAG 資料の再取込失敗時にデータが消えることがあった問題
- RAG 取込・埋め込みまわりの整合性と警告表示
- llama-server 停止が他プロセスに影響しうる問題（管理対象プロセスのみ停止）
- ストリーミング表示の UI スレッド安全性
- 設定画面のイベント購読リーク
- 依存パッケージ（SQLite）の脆弱性対応

## 1.0.0 - 2026-06-11

最初の公開候補バージョンです（GitHub 公開時にバージョンを確定）。

### 追加

- ローカル LLM（llama-server）とのストリーミングチャット
- キャラクター設定（名前・性格・話し方・生成パラメータ）
- RAG（PDF / Word / テキスト / HTML の資料検索）
- 画像添付と Vision 入力(mmproj 対応モデル使用時)
- URL 読み込み（Web ページ本文の 1 回限り添付）
- VOICEVOX 連携による読み上げ（自動起動・終了、話者選択）
- 初回起動時の自動セットアップ（llama.cpp・既定モデル・mmproj の取得）
- 日本語 / 英語 UI
- 設定画面からのバージョン表示・ライセンス情報・ログフォルダーへのアクセス
- 会話・設定・RAG データのバックアップ（ZIP エクスポート）

### 変更

- 初回ダウンロードの既定チャットモデルを Google の `gemma-4-E2B_q4_0-it.gguf` に変更
- 画像入力用 mmproj を同リポジトリの `gemma-4-E2B-it-mmproj.gguf` に揃えた

### 修正

- GPU 買い替え時に llama.cpp バックエンドを自動で入れ替える
- CUDA / AMD / Vulkan で VRAM が足りない場合、GPU レイヤー数を下げて再試行する
- 既定モデルサイズに合わせて起動前のメモリ見積もりを更新した

### 配布

- 公開 ZIP（framework-dependent）: `.\scripts\package-zip.ps1`
- .NET 同梱版: `.\scripts\publish-win.ps1 -BundleAllRuntimes`

---

書式の目安: 「追加 / 変更 / 修正 / 削除」の見出しでまとめ、利用者に影響のある内容のみを記載します。
