<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="LocalCompanion.Controls.ChatConversationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:LocalCompanion.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:LocalCompanion.ViewModels"
    mc:Ignorable="d">
    <ListView
        x:Name="MessageList"
        IsItemClickEnabled="False"
        Padding="0,0,12,8"
        SelectionMode="None"
        ShowsScrollingPlaceholders="False">
        <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="0,0,0,16" />
                <Setter Property="MinHeight" Value="0" />
            </Style>
        </ListView.ItemContainerStyle>
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="viewModels:ChatLineViewModel">
                <StackPanel Spacing="8">
                    <controls:ChatMessageBody
                        ApplySentenceBreaks="{x:Bind ApplySentenceBreaks, Mode=OneWay}"
                        HeaderText="{x:Bind Header, Mode=OneWay}"
                        SourceText="{x:Bind Text, Mode=OneWay}" />
                    <controls:ChatMessageBody
                        ApplySentenceBreaks="False"
                        SourceText="{x:Bind ReasoningText, Mode=OneWay}"
                        UseSecondaryForeground="True"
                        Visibility="{x:Bind ReasoningVisibility, Mode=OneWay}" />
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
